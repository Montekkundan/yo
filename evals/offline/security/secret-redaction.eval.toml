description = "Redacts provider keys and bearer credentials before retention or reporting."
kind = "redaction"
prompt = "AI_GATEWAY_API_KEY=gateway-test-token-cobalt719 Authorization: Bearer sk-example-cobalt719"
tags = ["offline", "security", "secrets"]
secrets = [
  "gateway-test-token-cobalt719",
  "sk-example-cobalt719",
]
